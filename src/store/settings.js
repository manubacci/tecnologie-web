import { reactive } from 'vue';

export const settings = reactive({
    theme: 'dark',
    color: '#FF0000',
    format: 'it-IT',
    seconds: true,
    invasive: false,
    order: 'update-descending'
});